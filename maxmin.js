const numbers = [45, 192, 25, 65, 55, 48, 77, 91, 312, 125];
function maxInArray(numbers) {

    let largest = numbers[0];
    for (i = 0; i < numbers.length; i++) {
        const index = i;
        const element = numbers[index];
        if (element > largest) {
            largest = element;
        }
        
        const largest = maxInArray
        console.log(largest);
    }
     else {('not found');
}
    }
    console.log(largest);



